# reference/ — current files, for context only (do NOT ship these as-is)

These are the **current, live** versions so you can feel the bar and copy the shared
player mechanics. Your job is to **replace** the three project films with richer,
bilingual versions, and to add the brand-new `my-resume.html`.

- `FILM-FORMAT-REFERENCE.html` — the **Film Contract** in working code: the 16:9 frame,
  4-scene cross-fade, play/pause + chapter-dot player (5000ms auto-advance), and the
  count-up logic. Match these mechanics in all four films. (It's a copy of `meta-ads.html`.)
- `meta-ads.html` · `al-maali.html` · `brand-system.html` — the current films you're
  upgrading. **They are English-only** today — your versions must add Arabic (`?lang=ar`)
  per the briefs, and lift the visuals.

What's missing on purpose: there's no current `my-resume` film — you're creating it.
