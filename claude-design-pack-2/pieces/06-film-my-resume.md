# Piece 06 — NEW Film: Career OS (My Resume)  →  `my-resume.html`

**Project:** Career OS · **Accent:** spectrum `#2997ff → #bf5af2` (blue→violet)
**This one is NEW** — the ninth story currently has **no film**. You're creating it from
scratch. Follow the **Film Contract** (16:9, 4 scenes, dots, 5000ms, count-ups, `?lang=ar`).

This is the most *meta* film: it's about **the site you're looking at** and how it was
made. It's the perfect note to end the set on.

---

## The story (true): built in the open — one person, three AI tools

This portfolio is itself a project: an **AI operating model**. One person (Mohamed) directs
**three AI tools** — **Cowork** (the controller: context, memory, briefs), **Claude Design**
(the look — only), and **Claude Code** (build, git, deploy) — to design and ship this very
site. He holds the context, writes the briefs, sets the no-fabrication rules, reviews every
result, and makes every decision. Nothing ships without him.

**Honesty (keep, important):** the AI tools **execute under his direction** — he designed the
operating model and made every call. Every number on the site is sourced; nothing is
fabricated. This page is about the **craft and the system**, not the job hunt.

> ⭐ A nice touch: this film literally credits **you** (Claude Design) as "the look." Feel
> free to make Scene 2 a small, elegant nod to the three-tool team — it's true.

## The 4 scenes

**Scene 1 · Built in the open**
- Eyebrow: EN `Career OS` / AR `نظام المسيرة`
- Headline: EN `This site is` + accent `the project.`
  AR `هذا الموقع هو` + accent `المشروع.`
- Sub: EN `One person directing three AI tools — to design and ship this very portfolio.`
  AR `شخصٌ واحد يُوجّه ثلاث أدوات ذكاء — لتصميم هذا الموقع وإطلاقه.`
- Visual: a single point of light (the person/controller) — calm, spectrum-tinted.

**Scene 2 · The operating model** (the three tools — a clean trio)
- Eyebrow: EN `The operating model` / AR `نموذج التشغيل`
- Three roles (a node each, the controller in the centre):
  - `Me + Cowork` — EN `controller · memory · briefs` / AR `أنا + Cowork` — `تحكّم · ذاكرة · مواجيز`
  - `Claude Design` — EN `the look — only` / AR `Claude Design` — `الشكل — فقط`
  - `Claude Code` — EN `build · git · deploy` / AR `Claude Code` — `بناء · git · نشر`
- Visual: controller in the middle directing two tools; arrows of direction flow **out** from
  the person (he briefs them), results flow back. Mirror in RTL.

**Scene 3 · How it ships** (the loop: direct → brief → review → ship)
- Eyebrow: EN `How it ships` / AR `كيف يُطلَق`
- Headline: EN `Direct. Brief.` + accent `Review. Ship.`
  AR `توجيه. تكليف.` + accent `مراجعة. إطلاق.`
- Sub: EN `Every handoff is a ready-to-paste brief — so work is never redone and context is never lost.`
  AR `كل تسليمٍ موجزٌ جاهزٌ للّصق — فلا يُعاد عملٌ ولا يضيع سياق.`
- Visual: a small cyclic flow (a loop of 4 steps) turning once, landing on "Ship".

**Scene 4 · The number**
- Eyebrow: EN `The number` / AR `الرقم`
- Big stat: `3 · 1 · 1` (three numerals, spectrum gradient) — set them so they read clearly.
- Caption: EN `AI tools · one workflow · one person.`
  AR `أدوات ذكاء · سير عمل واحد · شخص واحد.`
- A small support line of three count-ups underneath: `9 story pages` · `2 languages` · `1 master CV`
  / AR `٩ صفحات قصص` · `لغتان` · `سيرة ذاتية واحدة`

## Exact data (do not alter)
`3` AI tools (**Cowork, Claude Design, Claude Code**) · `1` workflow · `1` person · `9`
story pages · `2` languages (English · العربية, full RTL) · `1` master CV (tailored per role).

## Notes
- Spectrum accent (blue→violet) — it's the "system itself" story, so it gets the full palette,
  calmly. This is the finale of the film set: make it feel like a quiet, confident sign-off.
